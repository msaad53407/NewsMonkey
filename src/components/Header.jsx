import { SecondaryNavbar, Navbar } from "./";

const Header = () => {
  return (
    <header className="flex flex-col">
      <SecondaryNavbar />
      <Navbar />
    </header>
  );
};

export default Header;
